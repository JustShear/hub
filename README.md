# Just Shear Production Hub

A production application connecting Shopify orders to artwork proofing, production, warehouse picking, packing, freight, fulfilment, reprints and returns.

**Stage One** (current focus): proofing and print-export handover.

> **⚠ Not approved for production use.** One tracked blocker remains, not routine technical debt: Shopify's `shop/redact` compliance webhook has no automated data-erasure workflow yet, and `customers/redact` only covers a subset of the models that may hold a customer's data — see [ADR-0002](docs/decisions/0002-deferred-shopify-privacy-erasure.md) and the proposed **Privacy, Retention and Shopify Data Erasure** milestone. (The other former blocker — proof files/production artwork/export packages/freight labels on local disk rather than Cloudflare R2 — was resolved in Milestone 16; see [ADR-0011](docs/decisions/0011-cloudflare-r2-migration.md).) See [`docs/production-readiness-checklist.md`](docs/production-readiness-checklist.md) for the full go/no-go assessment.

## Source of truth

- [`docs/Just_Shear_Production_Hub_SRS_v1.1.docx`](docs/Just_Shear_Production_Hub_SRS_v1.1.docx) — the controlled Software Requirements Specification. Authoritative unless replaced by a later approved version (see its Section 1.1 change-control rule and Section 1.3 change log).
- [`docs/architecture/architecture-review.html`](docs/architecture/architecture-review.html) — pre-build architecture review: gaps closed, database schema, folder structure, and milestone plan (including the proposed Privacy/Erasure milestone).
- [`docs/development.md`](docs/development.md) — per-milestone developer documentation (Shopify scopes/webhooks, order-import architecture, retry/idempotency behaviour, known limitations).
- [`docs/decision-log.md`](docs/decision-log.md) — dated index of significant decisions, linking to full ADRs.
- [`docs/decisions/`](docs/decisions/) — architecture decision records (context, decision, limitations, risks, reconsideration triggers, required future work).
- [`docs/technical-debt.md`](docs/technical-debt.md) — open technical-debt register with status/severity.

## Status

**Milestones 01–16 complete and verified:**

- **01 — Repository and standards.** TypeScript (strict), React Router v8, ESLint, Prettier, Vitest, Playwright, GitHub Actions CI, fail-closed environment validation.
- **02 — Database foundation.** Full Prisma schema (~30 models per the architecture review), initial migration, seed data (roles, permissions, one admin login). Implemented ahead of sequence during Milestone 01's closeout and confirmed to stay.
- **03 — Authentication and roles.** Email+password staff sessions, RBAC permission checks, protected routes. No Shopify OAuth/App Bridge — per the SRS v1.1 change log, this is a standalone app with independent staff auth.
- **04 — Shopify order import.** GraphQL client, paginated order fetching, verified+idempotent webhooks (orders create/updated/cancelled plus the three mandatory privacy webhooks), the reusable `importShopifyOrder` service, OPTIS-property preservation with per-line artwork-asset linking, integration-failure tracking with exponential-backoff retry, and a dev CLI import tool. See [`docs/development.md`](docs/development.md) for the full architecture, scopes, webhook topics, and known limitations.
- **05 — Raw data inspector.** Developer-only screen (`/dev/orders`, gated behind a new `raw_data.view` permission) showing exactly what Milestone 04 imported: the raw Shopify payload and every line property — including OPTIS-style detected uploads and their linked artwork asset — with nothing filtered or reinterpreted.
- **06A — Application shell.** Reusable authenticated shell (header, permission-aware sidebar/mobile nav, user menu, breadcrumbs/page-header system, design tokens per SRS 19.1) that every page now renders inside; a dashboard landing page; a minimal read-only Integration Issues list; honest, non-functional shells for global search and notifications (no fake data); and a fix for the hardcoded dev seed password. See [`docs/development.md`](docs/development.md#application-shell-milestone-06a) for the shell architecture, how to add a nav item or page header, and the dev admin credential setup.
- **06B — Kanban board.** The main `/orders` operational board: 7 Stage One columns mapped from the existing `workflowStatus`/`proofSummary` enums (no schema replacement), accessible drag-and-drop (`@dnd-kit/core`) plus a keyboard-safe "Move to…" menu, server-authoritative status transitions with compare-and-swap idempotency and full audit logging, composable filters/sort/search, staff-specific saved views (existing `SavedView` model), a lightweight order preview (not the full drawer), and bounded/paginated queries designed for 1,000+ active orders. See [`docs/development.md`](docs/development.md#kanban-board-milestone-06b) for the column mapping, transition policy, performance strategy, and everything deferred to later milestones.
- **07 — Full order drawer.** The detailed internal order workspace opened from a Kanban card at a real, deep-linkable URL (`/orders/:orderId`), replacing the 06B preview dialog. Eight SRS-structured tabs (Overview, Products, Uploads, Notes, Shopify, Activity fully built; Proofs and Communication are honest placeholders); compare-and-swap editing of Hub-owned assignment/priority/due dates with full audit history; create-only internal notes; uploads grouped strictly by order line; a chronological activity timeline; and permission-controlled editing that never lets a Shopify sync overwrite a Hub-owned field. See [`docs/development.md`](docs/development.md#full-order-drawer-milestone-07) for the deep-linking architecture, permission model, editing/concurrency behaviour, and everything deferred to the proofing milestone. No proof-group creation, customer proof responses, export-for-print action, production/warehouse/packing screens, or Starshipit yet.
- **08 — Proof groups and proof versions.** The Proofs tab replaces its Milestone 07 placeholder: creating any number of proof groups per order, linking them to order lines and customer uploads (all genuinely many-to-many), a proof-required/not-required/undetermined decision with a reasoned, audited no-proof-required override, permanent versioned proof-file uploads (real file storage, magic-byte validation, checksum, concurrency-safe sequential version numbering, idempotent resubmission, never overwritten or deleted), internal readiness validation, artwork-staff assignment per group, internal notes, and an order-level proof summary calculated — never hand-set — from live proof-group state. See [`docs/development.md`](docs/development.md#proof-groups-and-proof-versions-milestone-08) for the full domain model, status state machines, and everything deferred to the customer-facing proofing milestone. No sending proofs to customers, customer approval/change-requests, production-artwork export, or production/warehouse/packing screens yet — and proof files live on local disk pending the Cloudflare R2 migration ([ADR-0004](docs/decisions/0004-interim-local-disk-proof-storage.md)).
- **09 — Customer proof portal and responses.** Staff can send one or more internally-ready proof groups to a customer as a single secure, expiring, revocable link (`/proof/:token`, outside the authenticated shell entirely). Customers review each group independently, approve (with an explicit confirmation checkbox — page-opening or link-clicking never counts) or request changes (with feedback and/or a marked-up file upload, stored separately from internal proof files), all as immutable, idempotent, concurrency-safe records. Real Klaviyo email delivery with retry/backoff via the existing integration-failure mechanism; one automatic reminder per request; resend/retry/revoke; a real client-side view-tracking event (never the page's own GET, so a mail-security scanner can't register a view or approve anything); an order-level proof summary and Kanban board that now reach `WAITING_ON_CUSTOMER`/`PARTIALLY_APPROVED`/`ALL_REQUIRED_PROOFS_APPROVED`/`CHANGES_REQUESTED` for real. See [`docs/development.md`](docs/development.md#customer-proof-portal-and-responses-milestone-09) for the full token/security design and everything deferred (export-for-print, production/warehouse/packing, Starshipit, returns, the customer-account extension).
- **10 — Export for print and production artwork.** The controlled handoff from a resolved proof group (customer-approved, or legitimately no-proof-required) into production-ready artwork. Staff prepare versioned, validated production artwork (PDF/PNG/TIFF/SVG/EPS/AI/EMB, separate from both the customer-facing proof file and any customer mark-ups), allocate it to order lines, and mark it ready for export. Export for print is a dedicated, audited action — never a Kanban drag — that runs as a two-phase reserve-then-finalise sequence: a real ZIP package (manifest + artwork files, no customer mark-ups/raw Shopify payloads/secrets) is built and stored, then a CAS-checked transaction atomically flips artwork to `EXPORTED` and the group to `EXPORTED_FOR_PRINT`, provably safe under concurrent export attempts. Re-export requires a documented reason and links to the prior batch; export history and packages are never regenerated or deleted. New Kanban indicators (`hasMissingProductionArtwork`, `hasReexportRequired`) and order-level `PARTIALLY_EXPORTED`/`ALL_REQUIRED_PROOFS_EXPORTED` states. See [`docs/development.md`](docs/development.md#export-for-print-and-production-artwork-milestone-10) for the full model, the two-phase export design, and everything deferred (the full production queue, warehouse, packing, Starshipit, returns, real-time cross-staff sync).
- **11 — Production queue and workstation workflow.** The internal production queue and per-job/per-task workstation that turns a Milestone 10 export batch into trackable production work — a `ProductionJob` (one per export batch/decoration-method pair) with one or more `ProductionTask`s (one per exported artwork item), each referencing the _exact_ artwork revision and export batch item that was actually exported, never the latest. Staff assign, start/pause/resume, record quantities (partial production, quality-check pass/fail/rework, documented overrides), report and resolve blocking/non-blocking issues, and complete work — with job status and the order's new `productionSummary` always derived from task state, never hand-set. A dedicated `/production` queue (named views, composable filters, priority-first default sort) and `/production/:jobId` workstation drawer; order-drawer, Kanban (`hasOpenProductionIssue`, `productionSummary`, assigned-staff indicators), dashboard, and basic date-range reporting integration. See [`docs/development.md`](docs/development.md#production-queue-and-workstation-workflow-milestone-11) and [ADR-0007](docs/decisions/0007-production-queue-workstation-model.md) for the full model, state machines, and everything deferred (warehouse picking, packing, Starshipit, returns, reprint claims, real-time cross-staff sync).
- **12 — Starshipit freight labels.** The first staff-triggered action that gets a completed order out the door: a `FreightShipment` (reserve-then-finalise, the same two-phase shape as Milestone 10's export batches) calls the real Starshipit API to create a shipment and print a label, stores the returned PDF, and writes the tracking number back to Shopify via `fulfillmentCreate` — the app's **first-ever Shopify write** after eight milestones of read-only access. Deliberately no packing gate this milestone (staff trigger it once an order is genuinely packed, gated only by permission — no `Packing` model exists yet); carrier/service/packaging-preset are free-text (no confirmed Starshipit discovery endpoint for an account's configured options); the Shopify sync is a separate, independently-failable step that folds into the existing `IntegrationFailure`/`STARSHIPIT` queue on failure, with `notifyCustomer: false` permanent this milestone. Order-drawer Freight tab (create/cancel/retry-sync/download) and a Kanban "Shipped" indicator. See [`docs/development.md`](docs/development.md#starshipit-freight-labels-milestone-12) and [ADR-0008](docs/decisions/0008-starshipit-freight-integration.md) for the full design and everything deferred (warehouse picking, packing, label voiding, returns, reprint claims).
- **13 — Warehouse picking.** Turns "production is done" into a tracked staff task to physically gather every item for an order and hand it to packing — a checklist workflow, not real inventory tracking (no SKU/bin on-hand-quantity model exists anywhere in this schema). A `WarehousePickJob` (one per order, real DB constraint) with one `WarehousePickItem` per order line auto-appears the moment `productionSummary` genuinely reaches `COMPLETE` (the creation hook lives inside `recalculateOrderProductionSummary` itself, in the same transaction — pure DB writes, so this is safe unlike Milestone 12's external Starshipit call). Staff record partial picks, mark a line short (auto-creates a non-blocking issue — a short pick never blocks handover, an explicit scope decision), report/resolve warehouse issues, assign work, and hand a job over to packing once every line is picked or marked short — finally making `OrderStatus.READY_TO_PACK` reachable (reserved since Milestone 06B). A dedicated `/warehouse` queue and `/warehouse/:jobId` workstation drawer; order-drawer Warehouse tab, Kanban (`hasOpenWarehouseIssue`, `hasShortPickItems`, a "Ready to pack" indicator), and dashboard integration. This is the milestone-numbering exception in this build log: described as "Milestone 12" in the architecture review's original suggested ordering (before Starshipit freight), but shipped after it per an earlier explicit user choice. See [`docs/development.md`](docs/development.md#warehouse-picking-milestone-13) and [ADR-0009](docs/decisions/0009-warehouse-picking.md) for the full design and everything deferred (real inventory tracking, bin locations, barcode scanning, the Packing milestone itself, returns, reprint claims).
- **14 — Returns, reprints & exceptions.** Everything that happens when something goes wrong after an order has otherwise moved through the pipeline — a unified `ExceptionCase` (classified by `category`: customer return, warranty claim, production defect, or other) with an investigation workflow (open → investigating ⇄ awaiting customer → resolved, or cancelled at any point) and one or more `ExceptionCaseResolution`s (reprint, store credit, refund, exchange, or denied). Refunds/credits are **record-only** — no Shopify refund/store-credit mutation, staff execute the actual transaction themselves; return labels are similarly manual/external, just a recorded fact. REPRINT and EXCHANGE resolutions both reuse the _existing_ Milestone 10 `createExportBatch`/`reExportBatch` mechanism rather than a second production-tracking model — a real `ExportBatch` → `ProductionJob`/`ProductionTask` chain results, same as any other re-export. A pre-existing, unused `Reprint`/`ReprintAsset` placeholder (reserved since before `ExportBatch`/`ProductionJob` existed) was removed in favour of this richer model, mirroring how Milestone 10 removed the similarly-stale `ProductionExport`. A dedicated `/exceptions` queue and `/exceptions/:caseId` workstation drawer (on the scale of Production/Warehouse, since "investigation workflow" was an explicit requirement); an order-drawer Exceptions tab with inline case creation; Kanban (`hasOpenExceptionCase` indicator) and dashboard integration. Any staff role can report a case; only Manager/Administrator can update/assign/resolve/cancel one. Numbering note: the user referred to this as "Milestone 15," following the architecture review's much finer-grained original numbering — it's Milestone 14 in this build log. See [`docs/development.md`](docs/development.md#exception-cases--returns-warranty-defects-milestone-14) and [ADR-0010](docs/decisions/0010-exception-cases.md) for the full design and everything deferred (real Shopify refund/credit execution, return-label generation, auto-completing resolutions tied to production status).
- **16 — Barcode, performance & release.** The closing "final polish" milestone: a real Cloudflare R2 storage adapter behind the existing provider-agnostic `StorageAdapter` interface, closing [ADR-0004](docs/decisions/0004-interim-local-disk-proof-storage.md) (factory-swapped by `NODE_ENV`, untested against a real bucket in this environment — see [ADR-0011](docs/decisions/0011-cloudflare-r2-migration.md)); a client-side polling-revalidation hook on every queue/board route (20-second interval, paused while backgrounded — narrows staleness, doesn't add real-time push); barcode scan-to-fill quantity entry, finally using the `Barcode`/`ScanEvent` models reserved since Milestone 02 (SKU-validated with a reasoned-override path for Warehouse, always-informational for Production — see [ADR-0012](docs/decisions/0012-barcode-scan-to-fill.md)); date-range reporting for Warehouse and Exceptions, mirroring Production's existing report page; real notifications (exception-case assignment, blocking warehouse/production issues) replacing the honest empty `NotificationMenu` shell; saved dashboards generalized across Production/Warehouse/Exceptions via a new `SavedView.scope` column and a query-string-based generic layer, distinct from the board's own typed filter schema; a manual security review (`docs/security-review.md`) that found and fixed one IDOR gap (a freight idempotency-key lookup missing `shopId` scoping) and added in-memory login rate limiting; backup/restore scripts drilled against the real local dev database (`docs/backup-and-restore.md`); a full deployment runbook (`docs/deployment.md`); and a production-readiness checklist (`docs/production-readiness-checklist.md`) whose bottom line is explicit: **this application is not yet approved for production** — the Shopify privacy/data-erasure milestone ([ADR-0002](docs/decisions/0002-deferred-shopify-privacy-erasure.md)) remains the one blocking item, deliberately out of scope here. The single-instance constraint ([ADR-0001](docs/decisions/0001-single-instance-job-execution.md)) is unchanged by this milestone's polling-refresh work.

`npm run typecheck`, `lint`, `format:check`, `test` (792 tests), and `build` all pass with zero errors. `npm audit` reports 0 vulnerabilities. Still no packing workflow, real inventory tracking, label voiding, or Shopify privacy/data-erasure — see the architecture review for the full milestone plan and [`docs/production-readiness-checklist.md`](docs/production-readiness-checklist.md) for the complete go/no-go assessment.

## Stack

- TypeScript (strict), React Router v8, standalone app — not an embedded Shopify admin app
- PostgreSQL + Prisma
- Cloudflare R2 for object storage (target; proof files, production artwork, export packages, and freight labels currently on local disk pending a future storage migration — see [ADR-0004](docs/decisions/0004-interim-local-disk-proof-storage.md))
- Klaviyo for customer-facing email (profile upsert + custom event tracking; Klaviyo Flows own the actual send)
- Starshipit for freight label generation (real API; carrier/service selection stays free text — no confirmed discovery endpoint)
- Hosted on Render
- Staff auth: email + password, admin-created accounts — no Shopify staff logins
- Vitest (unit/integration) + Playwright (e2e), ESLint + Prettier

## Authentication

Staff sign in at `/login` with an email + password created by an administrator (no self-signup, no Shopify staff accounts involved). A signed cookie session (`app/auth/staff-session.server.ts`) tracks who's signed in; every protected route's `loader` calls `requireStaffUser(request)`, which redirects to `/login?redirectTo=...` if there's no valid session and otherwise returns the staff user with their roles' permissions already flattened into a `Set`. `app/auth/rbac.ts`'s `hasPermission()` checks that set — it never touches the database itself, and has no `.server` suffix since components call it too (to decide what to render), so it's trivially unit-testable and safe in the client bundle.

`/logout` is a POST-only action (a form submission, not a link) so it can't be triggered by prefetch or a stray GET.

## Local setup

1. Install dependencies:

   ```bash
   npm install
   ```

2. Copy the environment template and fill in real values:

   ```bash
   cp .env.example .env
   ```

   The app validates every required variable at boot (`app/lib/env.server.ts`) and refuses to start if any are missing — see `.env.example` for what each one is and where it comes from. Optionally set `DEV_ADMIN_PASSWORD` if you want a password you choose rather than a generated one (also needed for `npm run test:e2e` — see below).

3. Start local Postgres (requires Docker Desktop) and apply migrations:

   ```bash
   docker compose up -d
   npm run db:migrate
   npm run db:seed
   ```

   This creates one seeded admin login: `admin@justshear.com`. There is no fixed default password — if `DEV_ADMIN_PASSWORD` was set in `.env` it's used, otherwise a random password is generated and printed to the console **once, the first time the admin account is created**. Re-running `db:seed` never changes an existing admin's password. Copy the printed password now if you didn't set `DEV_ADMIN_PASSWORD`.

4. Start the dev server and sign in with the seeded admin login above:

   ```bash
   npm run dev
   ```

   The app is available at `http://localhost:5173`.

## Scripts

| Script                         | Purpose                                                        |
| ------------------------------ | -------------------------------------------------------------- |
| `npm run dev`                  | Start the dev server with HMR                                  |
| `npm run build`                | Production build                                               |
| `npm run start`                | Run the production build                                       |
| `npm run typecheck`            | React Router type generation + `tsc`                           |
| `npm run lint`                 | ESLint                                                         |
| `npm run format`               | Prettier — write                                               |
| `npm run format:check`         | Prettier — check only (used in CI)                             |
| `npm run test`                 | Vitest (unit/integration)                                      |
| `npm run test:e2e`             | Playwright (e2e)                                               |
| `npm run db:generate`          | Regenerate the Prisma client after a schema change             |
| `npm run db:migrate`           | Create and apply a dev migration                               |
| `npm run db:migrate:deploy`    | Apply pending migrations (production)                          |
| `npm run db:seed`              | Seed roles, permissions and one admin login                    |
| `npm run db:studio`            | Prisma Studio                                                  |
| `npm run db:backup`            | Manual `pg_dump` snapshot to `.backups/`                       |
| `npm run db:restore --`        | Restore from a `db:backup` dump (destructive — confirms first) |
| `npm run import:order --`      | Manually import one Shopify order by GID or name               |
| `npm run register:webhooks --` | Register this app's webhook subscriptions with a shop          |

## Deployment

Targeting Render (managed Node hosting + managed Postgres). Runs as a **single Render Web Service** — the background job poller runs in-process inside that same service (see below), there is no separate worker process. A `Dockerfile` is present from the project scaffold if a containerized deploy is ever preferred instead — not required for the current Render plan. Full deployment documentation (env vars, prerequisites, build/deploy/migration/seed steps, rollback) is in [`docs/deployment.md`](docs/deployment.md).

> **Single-instance only.** Background job processing (`app/lib/job-poller.server.ts`) runs as an in-process poller with no cross-instance locking. Running more than one instance of this application concurrently may cause the same job to be processed more than once. Do not enable horizontal scaling/autoscaling until this is replaced per [ADR-0001](docs/decisions/0001-single-instance-job-execution.md). Import operations are idempotent wherever the schema allows it, which limits (but does not eliminate) the practical impact of duplicate processing in the meantime.
