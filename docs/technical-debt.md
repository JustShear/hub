# Technical debt register

Open items, tracked deliberately rather than left implicit in code comments alone. Each links to its full ADR where one exists.

| # | Item | Status | Introduced | ADR |
| --- | --- | --- | --- | --- |
| 1 | Shopify `shop/redact` has no automated data-erasure workflow; `customers/redact` only covers `ShopifyOrder` fields, not the ~10 other models that may carry a customer's data | **BLOCKING BEFORE PRODUCTION** | Milestone 04 | [ADR-0002](decisions/0002-deferred-shopify-privacy-erasure.md) |
| 2 | Background job processing runs on a single in-process poller with no cross-instance locking | Acceptable for single-instance deployment only | Milestone 04 | [ADR-0001](decisions/0001-single-instance-job-execution.md) |
| 3 | Addresses, discount codes, and fulfilments stored as opaque JSON, not normalised/queryable | Acceptable for current milestone | Milestone 04 | [ADR-0003](decisions/0003-opaque-commerce-json.md) |
| 4 | `ShopifyOrder.rawPayload` has a `rawPayloadPurgeAt` column but no job populates or acts on it — raw payloads are retained indefinitely in practice | Open, not yet blocking | Milestone 02 | — (folds into ADR-0002's retention-period scope) |
| 5 | ~~GraphQL query field names and OPTIS property-detection heuristics are unverified against a real Shopify store~~ — **Resolved**: verified by importing a real order (#36355) from the connected `just-shear.myshopify.com` store — correct customer/line data, correct FILE_UPLOAD vs TEXT property classification, correct artwork-to-line linking against real Shopify CDN URLs, zero integration failures. The `fulfilledQuantity` formula specifically remains unverified (no fulfilled order tested yet) | Partially resolved | Milestone 04 → post-M16 verification | — (see `docs/development.md` "Known limitations") |
| 6 | `package.json#prisma` seed config is deprecated (Prisma 7 will remove it); no `prisma.config.ts` migration done yet | Low priority, cosmetic warning only | Milestone 02 | — |
| 7 | Global search and the notification menu are honest, non-functional shells (no search backend, no notification source) | Acceptable — intentional scope boundary, not a defect | Milestone 06A | — (see `docs/development.md` "Application shell") |
| 8 | Kanban board: "Changes Requested"/"Proof Sent" columns and "Exported for Print" are not manually draggable; on-hold/cancelled/archived orders can't be reactivated from the board | Acceptable — depends on proof-group creation, the export action, and an override type that don't exist yet | Milestone 06B | — (see `docs/development.md` "Status-transition policy") |
| 9 | Kanban board has no real-time cross-staff sync — each staff member sees their own last-fetched state until they navigate or refetch | Open, not yet blocking (single-instance internal tool, low concurrent-edit risk today) | Milestone 06B | — (architecture review already floats polling as a Stage Two candidate) |
| 10 | `OrderNote` has no `edited`/`deletedAt` columns — the order drawer's Notes tab is create-only; editing or removing a note isn't possible yet | Acceptable — intentional scope boundary until the schema supports it | Milestone 07 | — (see `docs/development.md` "Internal notes") |
| 11 | `CustomerArtworkAsset` has no checksum field — the Uploads tab can't show one, and duplicate-content detection across differently-named uploads isn't possible | Open, not yet blocking | Milestone 07 | — (see `docs/development.md` "Uploads: grouped strictly by line") |
| 12 | Order drawer has no real-time cross-staff sync (same limitation as the board) — an edit by one staff member only appears to another after their next load/revalidation | Open, not yet blocking | Milestone 07 | — (see `docs/development.md` "Concurrency: revalidation, not just CAS") |
| 13 | Proof files are stored on local disk (`.storage/`), not Cloudflare R2 — no redundancy, no CDN, unsafe under horizontal scaling or an ephemeral filesystem | **Must be replaced before production go-live**; compounds ADR-0001's single-instance constraint | Milestone 08 | [ADR-0004](decisions/0004-interim-local-disk-proof-storage.md) |
| 14 | `proof-assets/:assetId` streams proof-file bytes directly through the Node process rather than redirecting to a short-lived signed URL | Open, tied to item 13 — resolves with the same R2 migration | Milestone 08 | [ADR-0004](decisions/0004-interim-local-disk-proof-storage.md) |
| 15 | `ProofRequirementValue.PARTIALLY_REQUIRED` and several `ProofGroupStatus`/`ProofVersionStatus` enum values (`SENT`, `VIEWED`, `CHANGES_REQUESTED`, `APPROVED`, `READY_FOR_EXPORT`, `EXPORTED_FOR_PRINT`) exist in the schema but are unreachable this milestone — no code path sets them yet | Acceptable — intentional scope boundary, reserved for the customer-proofing and export milestones | Milestone 08 | — (see `docs/development.md` "Proof groups and proof versions (Milestone 08)") |
| 16 | The customer proof-review URL (which embeds the raw token) is stored in `KlaviyoDispatch.eventProperties` so resend/reminders can re-deliver the exact same link without re-deriving the token. Since the order drawer's "View approval page" link (added after Milestone 09) now reads this same field back for staff, the URL is deliberately rendered to authorized internal staff — it is still never logged and still excluded from `IntegrationFailure.technicalDetail` | Acceptable — intentional, scoped exposure to internal staff who already have full order access; not a public leak | Milestone 09 | [ADR-0005](decisions/0005-proof-request-bundling.md) |
| 17 | No rate limiting on the public `/proof/:token` routes | Open, not yet blocking for a low-traffic internal-customer-base tool; revisit before wider exposure | Milestone 09 | — (see `docs/development.md` "Security summary") |
| 18 | `ProofReminder` has no stored "cancelled" state — a reminder that becomes moot before it's due (request completed/revoked/expired, or the order cancelled) is silently skipped by the poller each tick rather than being marked, so a staff-facing "why didn't this fire" view must re-derive the reason from the request/order's own status | Acceptable — intentional "derive, don't duplicate" design | Milestone 09 | — (see `docs/development.md` "The one automatic reminder") |
| 19 | Klaviyo delivery genuinely fails in every environment without a real `KLAVIYO_API_KEY` (local dev, CI) — every `KlaviyoDispatch` ends up `FAILED` and generates a real `IntegrationFailure` row | Acceptable — honest behaviour, not a bug; documented in the seed script and dev docs | Milestone 09 | — (see `docs/development.md` "Email delivery — Klaviyo") |
| 20 | Embroidery digitising files (`.emb`) are accepted by trusting the declared file extension, not a verified content signature — no single reliable public signature exists across vendor formats | Open, deliberate — rejecting a legitimate file the validator can't verify would be worse than trusting the extension for this one format | Milestone 10 | — (see `docs/development.md` "File validation — a much wider, and partly untrusted, format set") |
| 21 | `ShopifyOrder.workflowStatus` is only ever advanced forward by `createExportBatch` (into `PARTIALLY_EXPORTED`/`EXPORTED_FOR_PRINT`) — it is never re-derived downward when a group's artwork is later cancelled or a correction reverts the group back to `READY_FOR_EXPORT`, so it can read one step ahead of the group-level truth until the next export event recalculates it | Open, minor — `proofSummary` (the board's actual source of truth for proof-stage indicators) *is* fully re-derived on every mutation; only the coarser Kanban column position can lag | Milestone 10 | — (see `docs/development.md` "Order-level export summary and Kanban integration") |
| 22 | `export-batches/:exportBatchId/package`'s download count/timestamp are recorded as part of serving the GET response, so a browser prefetch or repeated load could slightly over-count | Acceptable — the count is explicitly informational only; nothing in the domain gates on it being exact | Milestone 10 | — (see `docs/development.md` "Routes and drawer UI") |
| 23 | `production_exports.override` permission is seeded but not wired to any bypass action — no code path exists yet for exporting without normal eligibility via the `ManualOverride` framework | Acceptable — intentional scope boundary; no genuine business need for it surfaced in the SRS this milestone | Milestone 10 | — (see `docs/development.md` "Permissions") |
| 24 | `ProductionTask.sequenceOrder`/`dependsOnTaskId` are stored but never enforced — no genuine multi-step (print → press → QC) decomposition exists yet, so every auto-created task is `GENERAL` and dependency ordering is inert | Acceptable — intentional scope boundary; reserved for a future milestone that needs real multi-step production | Milestone 11 | [ADR-0007](decisions/0007-production-queue-workstation-model.md) |
| 25 | `ProductionJob.assignedTeam` is a free-text label, not a `Team`/roster model | Acceptable — "where supported," not a full department feature this milestone | Milestone 11 | [ADR-0007](decisions/0007-production-queue-workstation-model.md) |
| 26 | The production queue has no real-time cross-staff sync (same limitation as the Kanban board and order drawer) — an assignment/status change by one staff member only appears to another after their next load/revalidation | Open, not yet blocking | Milestone 11 | — (see `docs/development.md` "Production queue and workstation workflow") |
| 27 | ~~The existing saved-view framework is not reused for the production queue~~ — **Resolved, Milestone 16**: `SavedView` gained a `scope` column; Production, Warehouse, and Exceptions each get real saved views via a new generic layer (`app/domain/saved-views/generic-saved-view.server.ts`) that stores the queue's own URL search params verbatim, rather than reusing the board's typed `BoardFilters` schema (which doesn't fit these queues' filter shapes) | Resolved | Milestone 11 → 16 | — (see `docs/development.md` "Production queue and workstation workflow"; new saved-dashboards section pending in Milestone 16 docs) |
| 28 | Kanban production indicators roll up from `ProductionJob`/`ProductionIssue` at card-render time with no caching — acceptable at current board sizes but worth revisiting if per-card query cost becomes measurable | Open, not yet blocking | Milestone 11 | — (see `docs/development.md` "Kanban integration") |
| 29 | `cancelFreightShipment` never calls a Starshipit void/cancel API — cancelling in the Hub only updates the internal record; the label may still be valid/billable with the carrier until voided manually outside the Hub | Open, deliberate — no void/cancel endpoint is confirmed in Starshipit's public API reference this milestone | Milestone 12 | [ADR-0008](decisions/0008-starshipit-freight-integration.md) |
| 30 | ~~The exact `/api/orders/shipment` request body was inferred from Starshipit's published response fields and a third-party integration write-up, not a fully published request schema~~ — **Resolved**: two real Australia Post labels were created against a real account (both cancelled, never manifested), confirming the request/response shapes for both order lookup and label creation. Uncovered a new narrow edge case in the process — see [ADR-0008](decisions/0008-starshipit-freight-integration.md)'s addendum (a retry against an order that's already had a label printed can still create a Starshipit-side duplicate; not fixed further, documented as a known limitation) | Resolved | Milestone 12 | [ADR-0008](decisions/0008-starshipit-freight-integration.md) |
| 31 | Carrier code, service level, and packaging preset are free-text staff input, not a validated dropdown — an invalid combination is only caught by Starshipit's own API response, not client-side | Acceptable — no confirmed Starshipit discovery endpoint exists for an account's actual configured carriers/services/presets | Milestone 12 | [ADR-0008](decisions/0008-starshipit-freight-integration.md) |
| 32 | No packing/warehouse gate exists before a freight label can be created — "Create freight label" is available as soon as `productionSummary === "COMPLETE"`, gated only by permission; staff are trusted to only trigger it once the order is physically packed | Acceptable — explicit user scoping decision this milestone, not an oversight | Milestone 12 | [ADR-0008](decisions/0008-starshipit-freight-integration.md) |
| 33 | No real inventory/stock-level tracking or bin-location model — picking is a checklist workflow, not a system that deducts from an actual warehouse stock count; `BarcodeType.BIN` remains just an enum value, not a genuine location model | Acceptable — intentional scope boundary; reserved for a future milestone that needs real inventory | Milestone 13 | [ADR-0009](decisions/0009-warehouse-picking.md) |
| 34 | ~~No barcode/scanner-driven pick confirmation~~ — **Resolved, Milestone 16**: scan-to-fill quantity entry now uses `Barcode`/`ScanEvent`, both for Warehouse (SKU-validated) and Production (informational). See [ADR-0012](decisions/0012-barcode-scan-to-fill.md). | Resolved | Milestone 13 → 16 | [ADR-0009](decisions/0009-warehouse-picking.md), [ADR-0012](decisions/0012-barcode-scan-to-fill.md) |
| 35 | Freight's own eligibility rule (Milestone 12) is not gated on `warehousePickSummary` — a freight label can in principle be created before an order's pick job is handed over to packing | Acceptable — explicit scope decision this milestone, matching Milestone 12's own no-packing-gate design; not an oversight | Milestone 13 | [ADR-0009](decisions/0009-warehouse-picking.md) |
| 36 | `WarehousePickJobStatus` has no distinct "ready for handover" value — a job with every line `PICKED`/`SHORT` still shows `IN_PROGRESS` until the explicit handover action fires, so the UI must derive "ready" from item states rather than reading it off the job's own status | Acceptable — intentional design; handover is always an explicit staff action, mirroring how `ProductionTask` completion works | Milestone 13 | [ADR-0009](decisions/0009-warehouse-picking.md) |
| 37 | ~~`markResolutionCompleted` never auto-completes a REPRINT/EXCHANGE resolution when its linked `ProductionJob` actually finishes~~ — **Superseded**: Production Jobs were removed entirely; REPRINT/EXCHANGE resolution is now a pure record-only no-op (same shape as CREDIT/REFUND/DENIED), with no real reprint/exchange artefact created anywhere. A genuine redesign is deferred until this flow is actually tested in practice | Open — deferred redesign, not yet scheduled | Milestone 14 → post-M16 removal | [ADR-0010](decisions/0010-exception-cases.md), [ADR-0014](decisions/0014-remove-production-artwork-and-jobs.md) |
| 38 | `ExceptionCaseAttachment` exists in the schema (mirrors `ProductionIssueAttachment`) but has no upload UI wired up this milestone | Open, not yet blocking | Milestone 14 | [ADR-0010](decisions/0010-exception-cases.md) |
| 39 | No Shopify refund/store-credit mutation, and no return-label generation API call — both are deliberate record-only/manual scope boundaries, not oversights | Acceptable — explicit user scoping decision this milestone | Milestone 14 | [ADR-0010](decisions/0010-exception-cases.md) |
| 40 | No signed-URL redirect for file downloads — every proof/artwork/export/freight-label route still streams bytes through the Node process rather than redirecting to a short-lived R2-signed URL | Acceptable — separate, larger change than this milestone's storage-provider swap; deferred | Milestone 16 | [ADR-0011](decisions/0011-cloudflare-r2-migration.md) |
| 41 | R2 adapter is untested against a real Cloudflare bucket in this environment — only a mocked-client unit test exists | Open, flagged for confirmation on first real deploy | Milestone 16 | [ADR-0011](decisions/0011-cloudflare-r2-migration.md) |
| 42 | A Shopify tag write (`syncOrderLifecycleTag`) succeeding right before a scheduled order re-sync runs could theoretically have the re-sync read stale tag data from Shopify's backend and overwrite the Hub's freshly-written local value backward, if Shopify's own propagation lags the mutation's response | Acceptable — generic eventual-consistency risk, not something this design can fully close; not observed in practice | Post-Milestone 16 | [ADR-0013](decisions/0013-kanban-lifecycle-tags-and-pack-column.md) |
| 43 | The inline Pack-column freight card only covers creating a new shipment — managing an existing one (cancel, retry Shopify sync, download label) still requires opening the full order drawer's Freight tab | Acceptable — explicit scope boundary, not asked for | Post-Milestone 16 | [ADR-0013](decisions/0013-kanban-lifecycle-tags-and-pack-column.md) |

## Item 1 detail — production-readiness blocker

**Do not approve this application for live production use until this is resolved.** A dedicated milestone, **"Privacy, Retention and Shopify Data Erasure,"** has been added to the milestone plan (`docs/architecture/architecture-review.html`) to resolve it. See [ADR-0002](decisions/0002-deferred-shopify-privacy-erasure.md) for the full model inventory and required scope.

## Item 2 detail — deployment constraint

**Horizontal scaling (more than one running instance of this application) must remain disabled** until the job-processing mechanism is replaced or strengthened per [ADR-0001](decisions/0001-single-instance-job-execution.md). This must be stated explicitly in the Milestone 23 deployment documentation, not left implicit.

## Item 13 detail — deployment constraint

**This application must also not be deployed to more than one instance, or to a platform without a guaranteed-persistent local disk, until the proof-file storage layer is migrated to Cloudflare R2** per [ADR-0004](decisions/0004-interim-local-disk-proof-storage.md). This compounds — does not replace — item 2's existing single-instance requirement.
